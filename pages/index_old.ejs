<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN"
      crossorigin="anonymous"
    />
    <title><%=title%></title>
  </head>
  <body>
    <%if(created){%>
    <div class="alert alert-success">"Note has been created!"</div>
    <%}%> <%if(error){%>
    <div class="alert alert-danger">"Somthing went wrong... "</div>
    <%}%>
    <div class="container">
      <h1><%=title%></h1>
      <form action="/" method="post" class="mb-2">
        <div class="form-group mb-2">
          <input type="text" class="form-control" name="inputText" required />
        </div>
        <button class="btn btn-primary" type="submit">Create!</button>
        <% if(notes.length>0) { %>
        <div class="card">
          <ul class="list-group">
            <% for (let i = 0; i < notes.length; i++) { %>
            <li
              class="list-group-item d-flex justify-content-between align-items-center"
              id="<%=notes[i].id%>"
            >
              <input
                type="text"
                id="inputTitle"
                name="inputTitle"
                disabled
                value="<%= notes[i].title%>"
                style="border: none; background-color: white; color: black"
              />
              <input
                type="text"
                id="oldInputTitle"
                name="oldInputTitle"
                value=""
                style="display: none"
              />
              <div id="editTitle" data-type="editTitle" style="display: none">
                <button
                  class="btn btn-primary"
                  data-type="save"
                  data-id="<%=notes[i].id%>"
                >
                  Сохранить
                </button>
                <button
                  class="btn btn-danger"
                  data-type="cancel"
                  data-id="<%=notes[i].id%>"
                >
                  Отменить
                </button>
              </div>
              <div id="viewTitle" data-type="viewTitle" style="display: block">
                <button
                  class="btn btn-primary"
                  data-type="edit"
                  data-id="<%=notes[i].id%>"
                >
                  Обновить
                </button>
                <button
                  class="btn btn-danger"
                  data-type="remove"
                  data-id="<%=notes[i].id%>"
                >
                  &times;
                </button>
              </div>
            </li>
            <% } %>
          </ul>
        </div>
        <%} else {%>
        <p>No notes!</p>
        <%}%>
      </form>
    </div>
    <script src="/app.js"></script>
  </body>
</html>
